# Demo script (≤3 minutes) — placeholder

1. Open the live Aegis UI (Vercel URL TBD).
2. Paste the Phoenix logistics brief: “Which of our Phoenix distribution yards crossed 35°C last July afternoon, and where should we restage loading?”
3. Show job status polling (`queued` → `planner` → `executor` → `analyzer` → `synthesizer`).
4. Open the LangSmith trace URL from the UI.
5. Scroll the cited memo: heatmap `activity_id`, tile max/mean, env_params heat index, recommended reroute polygon.
6. Close on credits remaining from `/v1/system/fetch-api-key-usage`.

Record after the graph + frontend increment; keep under 3:00.
